// Set up system variables
int window_width = 500;
int window_height = 500;
int window_x = 300;
int window_y = 200;
string window_title = "gpl Drench";
int animation_speed = 80; // 1 is slowest, 100 is fastest

// create the rectangles that represent the grid
rectangle grid[20];

initialization
{
    for (i = 0; i < 20; i += 1)
    {
        grid[i]
    }
}