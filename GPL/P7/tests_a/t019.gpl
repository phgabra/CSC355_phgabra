// random accepts INT and DOUBLE >= 1.0
// double values are rounded down
// The INVALID_ARGUMENT_FOR_RANDOM error is issued if argument < 1.0
//

int i;
int j;
int k;

// in p5, these errors terminated the program
// in p7, the INVALID_ARGUMENT_FOR_RANDOM error should be printed, 2 be used as the default 
//        and the execution should continue
on akey 
{
  i = random(-42);
  j = random(-12.345);
  k = random(0.345);
}
