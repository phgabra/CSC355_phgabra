int row;
int col;
string str;

on akey
{
    for (row = 0; row < 10; row = row + 1)
    {
        for (col = 0; col < 10; col = col + 1)
        {
            str = "row = " + row + ", col = " + col;
            print(str);
        }
    }
}

