// when there is an error in the forward statement, the animation_block
// is not inserted into the Symbol_table.
// That means if the body is provided, it will issue an error (no forward)

// Issues two errors:  
//  (1) bounce already declared
//  (2) no forward for animation bounce

int bounce;
forward animation bounce(rectangle cur_rectangle);

animation bounce(rectangle cur_rectangle)
{
  print("hello");
}

